<div id="<%= dom_id blog %>" class="card blog-card">
  <p>
    <strong>Title:</strong>
    <%= blog.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= blog.body %>
  </p>

  <% if blog.header_image.present? %>
    <%= image_tag blog.header_image %>
  <% end %>
  <%= blog.story %>

</div>
